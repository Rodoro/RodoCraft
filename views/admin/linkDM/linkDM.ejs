<%-contentFor('body') %>
  <h1>Привязка акаунтов</h1>
  <button><a href="/admin/">Назад</a></button>
    <ol>
      <% for(var i=0; i<users.length; i++){%>
          <li> 
            <%=users[i].discordName %>
            <a href="/admin/direction/linkDM/edit/<%= users[i].discordId %>">Редактировать</a>
          </li>
      <%} %>
    </ol>